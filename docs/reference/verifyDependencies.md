# Verifies if all dependencies are installed properly and instructs the user if this is not the case.

Verifies if all dependencies are installed properly and instructs the
user if this is not the case.

## Usage

``` r
verifyDependencies()
```
