#' @note When the object is altered (\emph{e.g.} by filtering or subsetting it), methods that need the original
#'   clustered data such as plotting methods do not work anymore and stop with an error.
