#' @param loadRawData Set to \code{TRUE} if analyses are available as \code{mzXML} or \code{mzML} files. Otherwise MS
#'   data is not loaded, and some dummy data (\emph{e.g.} file paths) is used in the returned object.
