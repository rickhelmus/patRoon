#' @param spectrumType A \code{character} vector which limits library records to the given spectrum types
#'   (\code{Spectrum_type} field, \emph{e.g.} \code{"MS2"}). Set to \code{NULL} to allow all spectrum types.
