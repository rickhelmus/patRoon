#' @param updateScore,<%=scoreWeightName%> If \code{updateScore=TRUE} then the annotation \code{score} column is updated
#'   by adding normalized values of the <%=scoreName%> (weighted by \option{<%=scoreWeightName%>}). Currently, this
#'   \strong{only} makes sense for annotations performed with \command{MetFrag}!
